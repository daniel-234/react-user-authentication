import { Label } from "reactstrap";
import styled from "styled-components";

// Define a styled component (as this is just to experiment,
// we'll use a color that is similar to the default one).
let StyledLabel = styled(Label)`
  color: #404040;
`;

export default StyledLabel;
